# Tokens (`SddIA/tokens/`)

Ruta canónica en minúsculas, alineada con `directories.tokens` en `SddIA/core/cumulo.paths.json`.

- **Contrato:** `tokens-contract.json`
- **Karma2Token:** `karma2-token/spec.json`

Los directorios legacy `Tokens/` (mayúscula) en repos antiguos están deprecados; migrar referencias a `SddIA/tokens/`.
