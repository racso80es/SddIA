[package]
name = "sddia-ecosystem-health"
version = "0.1.0"
edition = "2021"
description = "Read Model Espejo de Consciencia — fusión mapa × territorio"

[dependencies]
chrono = { version = "0.4", features = ["clock"] }
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3.10"
