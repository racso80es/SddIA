# Patrones de producto (`.sddia/patterns/`)

Patrones con **`nature: product`** (dominio de negocio del cliente). Misma forma que el motor: carpeta por UUID con `spec.md` + `spec.json` según `SddIA/patterns/patterns-contract.*`.
