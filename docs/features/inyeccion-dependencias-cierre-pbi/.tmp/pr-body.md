## Summary
- Finalize Done global PBI-042 tras merge PR #142 (`90424f4`).
- `finalize-process.md` + validacion merge refs (`merged_pr`, `merge_commit`, `PullRequest_Merged`).
- PBI-042 permanece en `docs/todos/done/` (`status: cerrado`).

## Test plan
- [x] PR #142 MERGED
- [x] PBI en `done/` ausente en `pending/`
- [x] `finalize-process.md` presente
- [ ] CI docs PR verdes