# Bóveda local SddIA — variables de instancia (no versionar .dev/.env con secretos)

# Comando CLI de inferencia LLM (Cursor u otro). Recibe prompt por stdin; responde por stdout.
# Ejemplo: cursor-agent --print
SDDIA_LLM_CLI_COMMAND=

# Timeout opcional para CLI LLM (segundos) — deuda: pendiente implementación en mayeuta-llm
# SDDIA_LLM_CLI_TIMEOUT_SECS=120

# Runtime de agentes V5 (slice B kalma2-full-cycle). Si está definido, fases solo-agent:
# reciben JSON AGENT_PHASE por stdin y deben emitir última línea JSON:
# {"success":true,"data":{"status":"executed|awaiting_agents|failed","message":"..."}}
# Lab: SddIA/scripts/tools/kalma2-agent-runtime-lab.sh (handoff + awaiting_agents;
#      SDDIA_AGENT_RUNTIME_LAB_AUTO=1 → executed tras registrar handoff).
# SDDIA_AGENT_RUNTIME_COMMAND=SddIA/scripts/tools/kalma2-agent-runtime-lab.sh
