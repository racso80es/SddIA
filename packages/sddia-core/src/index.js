/**
 * Shared Kernel SddIA — fachada npm (@sddia/core).
 * Stub F1: markers de jurisdicción. Runtime ejecutable = workspace Cargo / execute-process.
 */
"use strict";

/** @type {true} */
const SHARED_KERNEL = true;

/** @type {string} */
const SHARED_KERNEL_ID = "sddia-core";

/** @type {string} */
const SHARED_KERNEL_VERSION = "0.1.0";

/** @type {true} */
const DOMAIN_BLIND = true;

module.exports = {
  SHARED_KERNEL,
  SHARED_KERNEL_ID,
  SHARED_KERNEL_VERSION,
  DOMAIN_BLIND,
};
